
---

# E) Minimal `docs/DATASET_SETUP.md` (copy-paste)

```markdown
# Data Setup

This repository does **not** include raw CMU-MOSEI or MELD media.

## Required inputs
- CMU-MOSEI features: `data/processed/mosei/features/...`
- Manifests: `data/manifests/mosei_{train,val,test}.jsonl`
- (Optional) MELD test manifest mapped to emo6: `data/manifests/cross/meld_test_emo6.jsonl`

## Manifests
Each jsonl line should include at least:
- `utt_id`
- `text_raw` (or an equivalent text field)
- modality flags (`has_audio`, `has_video`, `has_text`)
- labels (`label_raw` or `csd` depending on your pipeline)
- feature pointers or keys used by the dataset loader

If you want qualitative panels with frames/audio, you must also provide:
- `video_path` or `frames_dir`
- `audio_path`
